# Dependencies
**/node_modules
.yarn/install-state.gz

# Testing
/coverage
/.jest-cache
/test-results

# Build directories
/dist/
/build/
/out/

# Misc
.DS_Store
*.pem

# Debug / logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
logs/

# Env files (keep the committed examples/templates, ignore real ones with secrets)
.env
.env.*
!.env.example
!.env*.template

# Vercel
.vercel

# TypeScript
*.tsbuildinfo

# Angular / build tooling caches
.angular/
.eslintcache

# Package manager
.npm

# IDE
.vscode/
.idea/
*.swp
*.swo
.idea

# Data files (can be regenerated)
/data/*.csv
.playwright-mcp
.serena/cache


# Methodology workspace (engineer's feature work)
# This will normally be gitignored in normal projects, but for the course we want to see it
# .claude/workspace/

# Methodology session handoffs (local context)
# This will normally be gitignored in normal projects, but for the course we want to see it
# .claude/meta/session-handoffs/

